apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: snapurl
  labels:
    app: backend
spec:
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  selector:
    app: backend
  type: ClusterIP
