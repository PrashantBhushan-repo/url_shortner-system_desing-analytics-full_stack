apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: snapurl
  labels:
    app: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: frontend
  type: ClusterIP
